name: Prepare Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version, for example 2.0.0'
        required: true
        type: string

permissions:
  contents: read
  pull-requests: read

jobs:
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: main
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24.14.1'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Verify generated runtime
        run: npm run build:check

      - name: Lint Markdown
        run: npm run lint:md

      - name: Run tests
        run: npm test

      - name: Validate release metadata
        env:
          RELEASE_VERSION: ${{ inputs.version }}
        run: npm run release:check -- "$RELEASE_VERSION"

      - name: Prepare release files
        env:
          RELEASE_VERSION: ${{ inputs.version }}
        run: npm run release:prepare -- "$RELEASE_VERSION"

      - name: Create release candidate pull request
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8.1.1
        with:
          token: ${{ secrets.RELEASE_PREP_PAT != '' && secrets.RELEASE_PREP_PAT || github.token }}
          branch: release-candidate/v${{ inputs.version }}
          delete-branch: true
          title: Prepare v${{ inputs.version }} release
          commit-message: Prepare v${{ inputs.version }} release
          body: |
            Prepares the release metadata for v${{ inputs.version }} from `main`.

            Review this PR and merge it through GitHub. Tag the exact resulting
            merge commit locally with GPG before running draft-release verification.
            Move the signed major tag only after verified immutable publication.
          add-paths: |
            CHANGELOG.md
            README.md
            package.json
            package-lock.json
